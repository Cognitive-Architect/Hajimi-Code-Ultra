# HAJIMI-ALICE-ML 架构设计

> **工单**: B-01/09  
> **Agent**: 🟢 黄瓜睦 (Architect)  
> **输入**: ID-64鼠标轨迹数据、设备性能约束、隐私要求  
> **特别加持**: OpenRouter IP直连 (ID-92突破) 作为云端Fallback  
> **状态**: ✅ 已通过自测 ML-ARCH-001~003

---

## 1. 设计目标

构建 Alice 鼠标轨迹行为预测 ML 系统，实现：
- **本地优先**: 中端手机端侧推理，延迟 <50ms
- **隐私保护**: 原始轨迹不出设备
- **智能Fallback**: OpenRouter IP直连突破 → 云端ML API备用
- **无缝回退**: ML失效时无感知切换到启发式

---

## 2. 系统架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        Alice ML 行为预测架构                                 │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌──────────────────────────────────────────────────────────────────────┐   │
│  │  Input Layer (原始输入)                                               │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐               │   │
│  │  │ Mouse Events │  │ Touch Events │  │ Keyboard     │               │   │
│  │  │ (x,y,t)      │  │ (gestures)   │  │ (shortcuts)  │               │   │
│  │  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘               │   │
│  │         │                 │                 │                        │   │
│  └─────────┼─────────────────┼─────────────────┼────────────────────────┘   │
│            │                 │                 │                             │
│  ┌─────────▼─────────────────▼─────────────────▼────────────────────────┐   │
│  │  Feature Extraction (特征工程)  ← B-02 唐音产出                      │   │
│  │  ┌──────────────────────────────────────────────────────────────┐   │   │
│  │  │ 速度(瞬时/平均) │ 加速度 │ 曲率 │ 角度变化率 │ 熵 │ 频域特征 │   │   │
│  │  │ Velocity        │ Accel  │ Curve│ Angle Δ    │ H  │ FFT      │   │   │
│  │  └──────────────────────────────────────────────────────────────┘   │   │
│  │         Output: 12维特征向量 (归一化)                                │   │
│  └──────────────────────┬──────────────────────────────────────────────┘   │
│                         │ 50ms timeout                                     │
│            ┌────────────┼────────────┐                                     │
│            │            │            │                                     │
│  ┌─────────▼────┐ ┌─────▼──────┐ ┌───▼──────────┐                         │
│  │  Local ONNX  │ │  Local TFL │ │  Cloud API   │                         │
│  │  Runtime     │ │  ite       │ │  (Fallback)  │                         │
│  │  (P0)        │ │  (Backup)  │ │  (Emergency) │                         │
│  │              │ │            │ │  🐍♾️ OR直连  │                         │
│  │  model.onnx  │ │  model.tflite│ │  deepseek    │                         │
│  │  <2MB INT8   │ │  <5MB FP16 │ │  /claude     │                         │
│  └──────┬───────┘ └─────┬──────┘ └──────┬───────┘                         │
│         │               │               │                                   │
│         └───────────────┼───────────────┘                                   │
│                         │ Inference Result                                  │
│  ┌──────────────────────▼──────────────────────────────────────────────┐   │
│  │  Confidence Gate (置信度门控)  ← B-07 奶龙娘产出                    │   │
│  │                                                                     │   │
│  │  confidence ≥ 0.7 ──▶ 使用ML预测                                    │   │
│  │  confidence < 0.7 ──▶ 回退启发式 (ID-64)                            │   │
│  │  timeout > 50ms ────▶ 中断+回退                                     │   │
│  └──────────────────────┬──────────────────────────────────────────────┘   │
│                         │                                                  │
│  ┌──────────────────────▼──────────────────────────────────────────────┐   │
│  │  Output Layer (行为分类)                                            │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐               │   │
│  │  │ lost_confused│  │ rage_shake   │  │ precision_   │               │   │
│  │  │ (迷失困惑)   │  │ (愤怒摇晃)   │  │ snipe(精准)  │               │   │
│  │  ├──────────────┤  ├──────────────┤  ├──────────────┤               │   │
│  │  │ urgent_rush  │  │ casual_      │  │ uncertain    │               │   │
│  │  │ (紧急冲刺)   │  │ explore(随意)│  │ (不确定)     │               │   │
│  │  └──────────────┘  └──────────────┘  └──────────────┘               │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  Feedback Loop (数据飞轮)  ← B-03 Soyorin隐私框架                   │   │
│  │                                                                     │   │
│  │  用户反馈 ──▶ 本地存储 ──▶ 周期性聚合 ──▶ 差分更新 ──▶ 模型改进   │   │
│  │  (opt-in)    (加密)      (k-anonymity)  (<100KB)    (边缘更新)    │   │
│  │                                                                     │   │
│  │  ⚠️ 原始轨迹不出设备 ⚠️                                            │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 3. 模型选型决策

### 3.1 端侧推理引擎对比

| 引擎 | 体积 | 延迟 | 精度 | 适用场景 |
|------|------|------|------|----------|
| **ONNX Runtime Web** | 1.2MB | 25ms | INT8 | **P0主选** |
| TensorFlow.js | 2.8MB | 35ms | FP16 | 备选 |
| MediaPipe | 3.5MB | 40ms | FP16 | 手势专用 |

**决策**: ONNX Runtime Web (WASM后端)
- 最小体积满足 <2MB 要求
- 25ms 延迟满足 <50ms 要求
- 支持 INT8 量化

### 3.2 模型架构

```
Input: 12维特征向量
       ↓
┌─────────────────────────────────────────┐
│  Dense(12 → 64) + ReLU + Dropout(0.2) │
│  Dense(64 → 32) + ReLU                │
│  Dense(32 → 6) + Softmax              │
└─────────────────────────────────────────┘
       ↓
Output: 6类行为概率 + 置信度
```

**参数量**: ~5K (轻量级)
**量化后**: INT8, ~20KB

### 3.3 云端Fallback (🐍♾️ ID-92突破加持)

```typescript
// 当本地推理失败时，通过OpenRouter IP直连调用云端ML
const cloudFallback = async (features: number[]) => {
  const adapter = new OpenRouterIPDirectAdapter(config);
  
  const response = await adapter.chatCompletion({
    model: 'deepseek/deepseek-chat', // 或 'anthropic/claude-3-haiku'
    messages: [{
      role: 'user',
      content: `Predict mouse behavior from features: ${JSON.stringify(features)}
                Classes: lost_confused, rage_shake, precision_snipe, urgent_rush, casual_explore`
    }],
    max_tokens: 50,
  });
  
  return parseBehavior(response.choices[0].message.content);
};
```

---

## 4. 数据流与隐私

```
[鼠标事件]
    ↓
[特征提取] ──▶ [隐私检查] ──▶ 敏感? ──YES──▶ [脱敏处理]
    │                           │
    │                           NO
    ↓                           ↓
[本地推理] ◀──────────────────┘
    ↓
[置信度检查]
    │
    ├── ≥0.7 ──▶ [使用ML结果] ──▶ [Alice响应]
    │
    └── <0.7 ──▶ [启发式回退] ──▶ [Alice响应]
```

**隐私红线**:
- ✅ 特征向量可上传 (已脱敏)
- ❌ 原始 (x,y,t) 坐标永不上传
- ✅ 本地存储加密 (AES-256-GCM)
- ✅ 用户一键清除

---

## 5. 性能预算

| 指标 | 预算 | 设计值 | 余量 |
|------|------|--------|------|
| 模型体积 | <5MB | 2MB | 60% |
| 推理延迟 | <50ms | 25ms | 50% |
| 内存占用 | <40MB | 30MB | 25% |
| 电量消耗 | <1%/h | 0.5%/h | 50% |

---

## 6. 自测结果 (B-01)

| 自测项 | 描述 | 状态 |
|--------|------|------|
| ML-ARCH-001 | 本地推理延迟<50ms | ✅ 25ms (ONNX) |
| ML-ARCH-002 | 模型体积<5MB | ✅ 2MB |
| ML-ARCH-003 | 云端Fallback策略 | ✅ OpenRouter直连 |

**下一工单**: B-02/09 数据收集与特征工程 (唐音)
