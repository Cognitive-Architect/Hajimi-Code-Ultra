{
  "name": "@hajimi/monorepo",
  "version": "0.9.1-rc",
  "description": "Hajimi Code Ultra - Monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "echo 'Build all packages'",
    "test": "npm run test --workspace=@hajimi/diff",
    "type-check": "npm run type-check --workspace=@hajimi/diff",
    "verify:all": "npm run verify:blake3 --workspace=@hajimi/diff && npm run verify:integrity --workspace=@hajimi/diff"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
